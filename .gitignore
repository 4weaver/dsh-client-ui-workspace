node_modules/
*.tsbuildinfo

# Build output. Regenerated by `npm run bundle` (build:types then tsdown);
# Nix's pkgs/dsh-client-ui-forkspace builds it from source. Never commit it:
# a committed bundle is not byte-reproducible from this repo and would mask
# whether the sources still build.
lib/
